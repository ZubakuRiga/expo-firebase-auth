# Expo Firebase Auth

A small Expo + Firebase example app demonstrating email/password and Google sign-in.

Quick start
1. Install dependencies:

   npm install

2. Start the app (Expo):

   npm run start

Firebase
- Firebase configuration lives in `firebase.js` — keep those credentials private.

